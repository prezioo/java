let miesiac=Math.floor(Math.random()15+1)
document.querySelector(#pierwszy).innerHTML=miesiac;
switch (miesiac){
    case 1:
    case 2:
    case 3:
        document.querySelector(#trzeci).innerHTML="kwartal I";
        console.log(miesiac)
        break;
        let miesiac=Math.floor(Math.random()15+1)
        document.querySelector(#pierwszy).innerHTML=miesiac;
    case 4:
    case 5:
    case 6:
        document.querySelector(#trzeci).innerHTML="kwartal II";
        console.log(miesiac)
    break;
        let miesiac=Math.floor(Math.random()15+1)
        document.querySelector(#pierwszy).innerHTML=miesiac;




}